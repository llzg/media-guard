services:
  media-guard:
    build: .
    container_name: media-guard
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml
    environment:
      - TZ=Asia/Shanghai
